import 'app_colors.dart';
import 'typography.dart';

class AppTheme {
  const AppTheme({
    required this.colors,
    required this.typography,
  });

  final AppColorsData colors;
  final WebTypographyData typography;
}
